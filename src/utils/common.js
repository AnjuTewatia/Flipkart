import {StyleSheet} from 'react-native';

const Common = StyleSheet.create({
  container: {
    flex: 1,
    // height: '60%',
    width: '100%',
    // backgroundColor: '#000',
  },
  formWrapper: {
    gap: 24,
  },
});
export default Common;
